jQuery(document).ready(function ($) {
    $('.banner-main-slider').slick({
        dots: true,
        arrows: false,
        adaptiveHeight: true,
        appendDots: $('.banner-main-slider-dots'),
    });
});